{% extends "AperoEventBundle::layout.html.twig" %}

{% block title %}
  Supprimer un Bar - {{ parent() }}
{% endblock %}

{% block AperoEvent_body %}

	<h3>Bar - {{ bar.nom }}</h3>

<div class='Ev'>
	<p>
    	Etes-vous certain de vouloir supprimer le bar "{{ bar.nom }}" ?
	</p>

	<form action="{{ path('apero_bar_delete', {'id': bar.id}) }}" method="post">
		<input type="submit" value="Supprimer"" />
		{{ form_rest(form) }}
	</form>
	<a href="{{ path('apero_bar_view', {'id': bar.id}) }}">
		<button>Retour à l'annonce</button>
	</a>
</div>
	
{% endblock %}